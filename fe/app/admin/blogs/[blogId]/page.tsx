import React from "react";
import parse from "html-react-parser";

// there would be display blog contents, along with images in the blog, displays just like the create, but we update blog now. (a.k.a. default values are filled in)

const page = () => {
    return <div>page</div>;
};

export default page;
